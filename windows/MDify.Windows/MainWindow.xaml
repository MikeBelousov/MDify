<Window x:Class="MDify.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MDify"
        Height="760"
        Width="1160"
        MinHeight="560"
        MinWidth="880"
        AllowDrop="True"
        DragEnter="Window_DragEnter"
        Drop="Window_Drop">
  <DockPanel Background="#F7F7F5">
    <ToolBarTray DockPanel.Dock="Top" Background="#FFFFFF">
      <ToolBar>
        <Button Content="Add Files" Command="{Binding AddFilesCommand}" Margin="2" Padding="10,4" />
        <Button Content="Add Folder" Command="{Binding AddFolderCommand}" Margin="2" Padding="10,4" />
        <Button Content="Output" Command="{Binding ChooseOutputFolderCommand}" Margin="2" Padding="10,4" />
        <Separator />
        <Button Content="Convert All" Command="{Binding ConvertAllCommand}" Margin="2" Padding="10,4" />
        <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="2" Padding="10,4" />
        <Separator />
        <Button Content="Copy" Command="{Binding CopyMarkdownCommand}" Margin="2" Padding="10,4" />
        <Button Content="Show in Explorer" Command="{Binding RevealOutputCommand}" Margin="2" Padding="10,4" />
        <Separator />
        <Button Content="Remove" Command="{Binding RemoveSelectedCommand}" Margin="2" Padding="10,4" />
        <Button Content="Clear Completed" Command="{Binding ClearCompletedCommand}" Margin="2" Padding="10,4" />
      </ToolBar>
    </ToolBarTray>

    <StatusBar DockPanel.Dock="Bottom" Background="#FFFFFF">
      <StatusBarItem Content="{Binding StatusText}" />
      <Separator />
      <StatusBarItem>
        <TextBlock Text="{Binding OutputFolder}" TextTrimming="CharacterEllipsis" />
      </StatusBarItem>
    </StatusBar>

    <Grid Margin="12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" MinWidth="340" />
        <ColumnDefinition Width="3*" MinWidth="420" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Border Grid.Column="0"
              Background="#FFFFFF"
              BorderBrush="#D8D8D2"
              BorderThickness="1"
              CornerRadius="6"
              Margin="0,0,12,0">
        <DataGrid ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  BorderThickness="0">
          <DataGrid.Columns>
            <DataGridTextColumn Header="File" Binding="{Binding DisplayName}" Width="2*" />
            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
            <DataGridTextColumn Header="Folder Path" Binding="{Binding FolderRelativeDisplayPath}" Width="2*" />
          </DataGrid.Columns>
        </DataGrid>
      </Border>

      <Border Grid.Column="1"
              Background="#FFFFFF"
              BorderBrush="#D8D8D2"
              BorderThickness="1"
              CornerRadius="6">
        <TabControl BorderThickness="0">
          <TabItem Header="Markdown">
            <TextBox Text="{Binding PreviewText}"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="12"
                     BorderThickness="0"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" />
          </TabItem>
          <TabItem Header="Log">
            <TextBox Text="{Binding LogText}"
                     AcceptsReturn="True"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="12"
                     BorderThickness="0"
                     VerticalScrollBarVisibility="Auto" />
          </TabItem>
        </TabControl>
      </Border>
    </Grid>
  </DockPanel>
</Window>
